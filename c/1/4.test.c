include <stdio.h>

    int main(){
        char str[]="hello kaikeba nihao"
        char temp[4][100]={0}
        sscanf(str,"%s %s %s %s",temp[0],temp[1],temp[2],temp[3])
        for(int i=0;i<4;i++){
          printf
        }
        scanf("%d%d%d")
    }